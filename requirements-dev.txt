# Development / test-only dependencies.
# Install together with runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Used by the CI test job and local test runs:
#   python -m pytest tests/
pytest>=8.0
pytest-django>=4.8
# pytest-env reads the `env =` section in pytest.ini (DB_ENGINE, SECRET_KEY,
# ALLOWED_HOSTS) so tests run headless and offline without a real .env.
# >=1.6 because older 1.1.x predates pytest 8 compatibility.
pytest-env>=1.6
# Linter + formatter (config in ruff.toml: E+F rules).
# Run with: ruff check . && ruff format --check .
ruff>=0.16
